
<select name="userToInvite" id="userToInvite">

{{#each userList}}
<option value="{{_id}}"> {{username}} </option>
</select>


<select name="projectToInvite" id="projectToInvite">

{{#each projects}}
<option value="{{_id}}"> {{name}} </option>
</select>




<select name="fileToInvite" id="fileToInvite">

{{#each uploads}}
<option value="{{_id}}"> {{name}} </option>
</select>

